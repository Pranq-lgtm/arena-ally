import Time "mo:core/Time";

module {
  public type UserId = Principal;
  public type Timestamp = Int; // Time.now() nanoseconds
  public type Sport = Text; // e.g. "basketball", "soccer", "tennis"
};
